# OS / editor cruft
.DS_Store
Thumbs.db
desktop.ini
*.swp
.vscode/

# Secrets — never commit these
.env
.env.*
*.key
*.pem

# Node
node_modules/

# Full-res source photos — keep locally, never commit (they bloat git history)
originals/*
!originals/README.md
